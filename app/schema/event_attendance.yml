name: event_attendance
properties:
  - name: event_id
    type: integer # relation to event table
    belongs_to: event
  - name: attendant_id
    type: integer # relation to user table
    belongs_to: user
  - name: joined
    type: boolean